version: "3.8"

services:
  nginx:
    image: nginx
    container_name: nginx
    network_mode: host
    volumes:
      - ./conf.d/localhost.conf:/etc/nginx/conf.d/localhost.conf:ro
      - ./certs:/etc/nginx/certs:ro

